# Zyen Hub Free Download Site

Put the final tested APK in this folder as `ZyenHub.apk`.

Then upload `index.html`, `icon-512.png`, and `ZyenHub.apk` to a free static host such as GitHub Pages.

The download button already points to `ZyenHub.apk`.
